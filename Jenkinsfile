# Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { docker 'python:3.6.4' }
    stages {
        stage('build') {
            steps {
                'python --version'
            }
        }
    }
}